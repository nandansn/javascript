var age = 20;

var qualifiedTpVote = (age < 18) ? false:true

if (qualifiedTpVote) {
    document.write("Qualified")
} else{
    document.write("Not Qualified")
}

document.writeln(typeof(age))